# Optional: override the API server origin (default: same origin via Vite proxy)
# Uncomment and set this when the API server is on a separate origin in production.
# VITE_API_BASE_URL=https://api.codefind.dev
